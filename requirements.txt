streamlit==1.44.1                  # Latest stable Streamlit that works with TF 2.16
streamlit-webrtc==0.48.2           # Compatible with Python >=3.7,<3.13
tensorflow==2.16.0                 # Matches your requirement
opencv-python-headless==4.9.0.80   # Verified stable version for TF/mediapipe compatibility
mediapipe==0.10.10                 # Compatible with TF 2.16 and OpenCV-headless
numpy==1.24.3                      # TensorFlow 2.16 recommends 1.23 to 1.26 range
pyopengl==3.1.5                    # Optional, if using OpenGL (leave it out if not needed)
